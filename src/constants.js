const ViewingState = {
  viewing: 0,
  zooming: 1,
  splitVote: 2
}

const EditingState = {
  viewing: 2,
  editing: 3
}

const LoaderType = {
  hidden: 0,
  standard: 1,
  progress: 2
}

const EditingMode = {
  margin: 0,
  voteshare: 1
}

const CompareSortMode = {
  voteshare: 0,
  shiftMargin: 1
}